# Minimal credentials for testing — copy to .env
# Only OPENAI_API_KEY is required. Reddit runs without any key.
# YouTube, Apify, Tavily, Google Sheets are skipped when not set.

OPENAI_API_KEY=sk-...
